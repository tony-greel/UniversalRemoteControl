# 新需求 / 功能记录 — 2026

> 由开发者或维护者人工追加。最新在上。

<!-- 新条目追加在此行下方 -->

## 2026-08-17 全量变量中文注释补强

- **需求描述**：在已有方法级 KDoc 基础上，为项目主源码中的**变量**补充中文注释，便于阅读协议常量、LiveData 状态、data class 字段与关键局部变量。
- **验收标准**：
  - 22 个 `app/src/main` Kotlin 文件：属性、枚举值、构造参数、companion 常量、关键局部变量均有中文说明
  - 仅注释变更，业务逻辑不变；`./gradlew assembleDebug` 通过
- **规则链路**：
  - 无运行时链路变更；注释覆盖与架构文档一致的分层：`domain/model` 字段 → `data/protocol/tcl` 协议常量 → `domain/repository` 内存状态 → `ui` LiveData / suppress 标志
- **涉及模块**：
  - `domain/model` — `AcState`、`DeviceProfile`、`LightControlConfig` 等字段与枚举项
  - `data/protocol/tcl` — `Tcl112AcEncoder` 时序常量、`Tcl112AcRemote` 字节下标与 bit 掩码
  - `data/preference` — SharedPreferences KEY 含义
  - `domain/repository` — 依赖注入参数、`acState`/`lightConfig` 内存变量
  - `data/ir` — `IrPattern` 字段
  - `ui/main`、`ui/ac` — `binding`、`_devices` 等 LiveData、`suppress*Callback`
- **改动文件**：
  - 修改（变量/常量注释，无逻辑变更）：与「项目架构说明与全量方法中文注释」相同的 22 个主源码文件（`UniversalRemoteApp.kt`、`core/`、`domain/`、`data/`、`ui/`）
- **对原有功能的影响**：无
- **后续待办**：
  - 可选：`app/src/test` 测试类变量注释
  - 与上条 Feature 合并维护时注意避免重复归档同类纯注释改动

## 2026-08-17 项目架构说明与全量方法中文注释

- **需求描述**：为 onboarding 与后续迭代提供可检索的架构说明；在全部主源码 Kotlin 类的每个方法上补充中文 KDoc（做什么、有什么用、调用链、修改哪个参数）。
- **验收标准**：
  - `docs/architecture.md` 含分层图、主调用链、类职责速查、常见修改入口表
  - 22 个 `app/src/main` Kotlin 文件的方法级中文注释齐全
  - 仅注释与文档，不改变业务逻辑；`./gradlew assembleDebug` 通过
- **规则链路**：
  - 文档入口：`docs/architecture.md` ↔ 各层包结构（`ui` / `domain` / `data` / `core`）
  - 遥控主链（文档中描述）：`AcRemoteActivity` → `AcRemoteViewModel` → `RemoteControlRepository` → `Tcl112AcRemote` / `TclLightPatternLibrary` → `AndroidIrTransmitter`
  - 首页配对链：`MainActivity` → `MainViewModel` → `RemoteControlRepository.checkDeviceConnection` / `sendConnectionProbe`
- **涉及模块**：
  - `docs` — 架构总览文档
  - `UniversalRemoteApp`、`core` — 应用入口与 DI
  - `domain/model`、`domain/repository` — 模型与编排层注释
  - `data/ir`、`data/protocol/tcl`、`data/preference` — 硬件/协议/持久化注释
  - `ui/main`、`ui/ac`、`ui/ViewModelFactory` — UI 与 ViewModel 注释
- **改动文件**：
  - 新增：`docs/architecture.md`
  - 修改（KDoc 注释，无逻辑变更）：`UniversalRemoteApp.kt`、`core/AppContainer.kt`、`domain/model/*.kt`、`domain/repository/RemoteControlRepository.kt`、`data/ir/*.kt`、`data/protocol/tcl/*.kt`、`data/preference/*.kt`、`ui/**/*.kt`（共 22 个主源码文件）
- **对原有功能的影响**：无；注释与文档仅提升可读性，IR 发送、配对、灯光实验等行为未改
- **后续待办**：
  - 可选：测试类（`app/src/test`）同步补充中文注释
  - 可选：类依赖关系图写入 `docs/architecture.md`
  - UI 能力选型（Compose 增量引入）待用户审查后另开 Feature 条目

## 2026-08-17 设备连接探测与设置入口

- **需求描述**：进入主页前先判断手机是否具备红外通信能力；设备列表每项增加「设置」按钮，点击时探测连接，成功进入空调遥控页，失败则按钮显示「连接失败，需重试」。
- **验收标准**：
  - 无 IR 硬件时主页显示拦截页 + 重试按钮，不展示设备列表
  - 有 IR 时展示设备列表，每项右侧有「设置」按钮
  - 点击「设置」且探测通过 → 进入 `AcRemoteActivity`
  - 探测失败 → 按钮文案变为「连接失败，需重试」，再次点击可重试
  - `./gradlew assembleDebug test` 通过
- **规则链路**：
  - 主页门禁：`MainActivity.onCreate` → `MainViewModel.checkPhoneCapability()` → `RemoteControlRepository.hasIrEmitter()` → 显示 `panelIrGate` / `panelDeviceList`
  - 设置入口：`DeviceListAdapter.btnSettings` → `MainViewModel.requestOpenDeviceSettings()` → `RemoteControlRepository.checkDeviceConnection()` → 成功 `startActivity(AcRemoteActivity)` / 失败更新 `connectionFailedDeviceIds`
  - 失败回显：`connectionFailedDeviceIds` LiveData → `submitConnectionFailures()` → 按钮文案切换
- **涉及模块**：
  - `domain/model` — `DeviceConnectionResult`
  - `domain/repository` — `checkDeviceConnection()`
  - `ui/main` — 主页门禁、列表设置按钮、连接失败态
  - `res/layout` — `activity_main.xml`、`item_device.xml`
- **改动文件**：
  - 修改：`app/src/main/java/com/universalremote/control/domain/model/Models.kt`
  - 修改：`app/src/main/java/com/universalremote/control/domain/repository/RemoteControlRepository.kt`
  - 修改：`app/src/main/java/com/universalremote/control/ui/main/MainViewModel.kt`
  - 修改：`app/src/main/java/com/universalremote/control/ui/main/MainActivity.kt`
  - 修改：`app/src/main/java/com/universalremote/control/ui/main/DeviceListAdapter.kt`
  - 修改：`app/src/main/res/layout/activity_main.xml`
  - 修改：`app/src/main/res/layout/item_device.xml`
  - 修改：`app/src/main/res/values/strings.xml`
  - 修改：`app/src/test/java/com/universalremote/control/domain/repository/RemoteControlRepositoryTest.kt`
- **对原有功能的影响**：无；移除整卡点击进入遥控，改为仅「设置」按钮入口；IR 协议与 `AcRemoteActivity` 未改
- **限制（事实）**：IR 单向通信，连接探测仅校验手机红外发射器可用，无法确认空调本体在线
- **后续待办**：
  - 可选：卡片点击恢复快捷进入（同样走连接探测）
  - 可选：网络/蓝牙设备接入后扩展 `checkDeviceConnection` 分支

## 2026-08-17 空调状态持久化：进入 App 恢复上次键位设置

- **需求描述**：用户希望 App 进入空调遥控页时，模式/温度/风速/开关/灯光等键位设置与上次操作一致，而非每次重置为协议默认值。
- **验收标准**：
  - 每次通过 App 发送遥控指令后，空调状态（`AcState`）自动持久化
  - 冷启动或重新进入遥控页时，UI 高亮与状态栏文案与上次保存的状态一致
  - `./gradlew assembleDebug test` 通过
- **规则链路**：
  - 写入：用户操作 → `RemoteControlRepository.sendAcAction()` / 灯光相关发送 → `updateAcState()` → `AcStateStore.save()`
  - 读取：`AppContainer` 创建 `RemoteControlRepository` → `loadPersistedAcState()` → `Tcl112AcRemote.restore()` → `AcRemoteViewModel` 初始 `LiveData` → `renderState()`
- **涉及模块**：
  - `data/preference` — `AcStateStore` 接口 + `AcStatePreferences`（SharedPreferences）
  - `domain/repository` — 状态加载/持久化编排
  - `data/protocol/tcl` — `Tcl112AcRemote.restore()` 恢复内部字节状态
  - `core` — `AppContainer` 注入
- **改动文件**：
  - 新增：`app/src/main/java/com/universalremote/control/data/preference/AcStateStore.kt`
  - 新增：`app/src/main/java/com/universalremote/control/data/preference/AcStatePreferences.kt`
  - 修改：`app/src/main/java/com/universalremote/control/domain/repository/RemoteControlRepository.kt`
  - 修改：`app/src/main/java/com/universalremote/control/data/protocol/tcl/Tcl112AcRemote.kt`（`restore()`）
  - 修改：`app/src/main/java/com/universalremote/control/core/AppContainer.kt`
  - 修改：`app/src/test/java/com/universalremote/control/domain/repository/RemoteControlRepositoryTest.kt`（恢复 + 持久化单测）
- **对原有功能的影响**：无；IR 编解码、UI 布局、发送逻辑不变，仅增加状态持久化层
- **限制（事实）**：App 仅 IR 发送、无红外接收；若用户用实体遥控器改空调，App 无法自动感知，需后续加「手动同步」或 IR 学习/解码
- **后续待办**：
  - 可选：首次进入引导用户确认当前空调实际状态
  - 可选：按 `DeviceProfile.id` 分设备存储状态

## 2026-08-17 空调模式与风速单选高亮

- **需求描述**：空调遥控页的模式（制冷/制热/自动/送风）与风速（自动/低/中/高）按钮需有单选标识，便于用户识别当前选中项。
- **验收标准**：
  - 发送模式/风速指令成功后，对应按钮高亮（蓝色实心底 + 白字），其余为描边样式
  - 进入页面时根据 `AcState` 初始状态正确显示选中项
  - 关机状态下仍保留上次选中态（状态栏显示「已关机」）
  - `./gradlew assembleDebug` 通过
- **规则链路**：
  - 点击：`AcRemoteActivity` 模式/风速按钮 → `AcRemoteViewModel.sendAction(RemoteAction.*)` → `RemoteControlRepository.sendAcAction()` → 更新 `AcState.mode` / `AcState.fanSpeed`
  - 回显：`ViewModel.acState` LiveData → `renderState()` → `updateModeSelection()` / `updateFanSelection()` → `MaterialButton.isSelected`
  - 视觉：`isSelected=true` → `AcRemote.ToggleButton` 样式 → `ac_toggle_*` ColorStateList（背景/文字/描边）
- **涉及模块**：
  - `ui/ac` — 状态观察与选中态同步
  - `res/layout` — 模式/风速按钮样式替换
  - `res/values` — 切换按钮主题样式
  - `res/color` — 选中/未选中 ColorStateList
- **改动文件**：
  - 修改：`app/src/main/java/com/universalremote/control/ui/ac/AcRemoteActivity.kt`（`updateModeSelection`、`updateFanSelection`）
  - 修改：`app/src/main/res/layout/activity_ac_remote.xml`（8 个按钮改用 `AcRemote.ToggleButton`）
  - 修改：`app/src/main/res/values/themes.xml`（新增 `AcRemote.ToggleButton` 样式）
  - 新增：`app/src/main/res/color/ac_toggle_background_tint.xml`
  - 新增：`app/src/main/res/color/ac_toggle_text_color.xml`
  - 新增：`app/src/main/res/color/ac_toggle_stroke_color.xml`
- **对原有功能的影响**：无；仅 UI 层增加选中态视觉反馈，红外协议、Repository、ViewModel 发送逻辑未改
- **后续待办**：
  - 可选：状态栏文案补充当前风速
  - 可选：关机时降低未选中按钮透明度以区分可操作态

## 2026-08-17 灯光控制可靠性验证与单测补强

- **需求描述**：用户暂无法真机验证，要求在代码层确认灯光控制（尤其方式三 HYBRID）是否可靠；补齐单元测试覆盖候选码生成与 Repository 发送编排。
- **验收标准**：
  - `./gradlew assembleDebug test` 通过
  - 6 种灯光独立候选码对开/关均能生成有效 38kHz 波形，且同候选开/关波形不同
  - HYBRID 模式下一次关灯/开灯串行发送 `1 + candidates.size`（7）次
  - `hybridAttemptSummary()` 包含全部候选标签
  - 无 IR 硬件时返回 `TransmitResult.Failure`
- **规则链路**：
  - 候选码验证：`TclLightPatternLibraryTest` → `TclLightPatternLibrary.patternFor()` → `Tcl112AcEncoder.encode()`
  - 混合轮询验证：`RemoteControlRepositoryTest` → `sendAcAction(LIGHT_*)` → `sendHybridLight()` → 假 `IrTransmitter` 计数
  - 配置注入：`LightControlConfigStore` ← `InMemoryLightControlConfigStore`（测试）/ `LightControlPreferences`（运行时）
- **涉及模块**：
  - `data/protocol/tcl`（测试）— 候选库编码覆盖
  - `domain/repository`（测试）— HYBRID 发送次数、摘要、无 IR 失败路径
  - `data/preference` — `LightControlConfigStore` 接口抽象
- **改动文件**：
  - 新增：`data/preference/LightControlConfigStore.kt`
  - 新增：`app/src/test/.../TclLightPatternLibraryTest.kt`（4 项）
  - 新增：`app/src/test/.../RemoteControlRepositoryTest.kt`（4 项）
  - 修改：`LightControlPreferences.kt`（实现接口）、`RemoteControlRepository.kt`（依赖接口类型）
- **对原有功能的影响**：无行为变化（接口抽取与单测）；与 HYBRID 提前返回 bug 修复配合，方式三逻辑与测试一致
- **后续待办**：
  - 真机验证哪种策略对具体 TCL 空调生效
  - 可选：方式三发送间隔、成功后停止后续轮询
  - 真机 IR 响应无法由单测替代，需用户反馈有效候选 ID

## 2026-08-17 灯光控制方式三：HYBRID_ALL 自动轮询落地

- **需求描述**：在无原装遥控、未知空调型号时，用户希望一次点击「关灯/开灯」即自动串行尝试多种灯光策略，无需手动逐个切换方式一/方式二。
- **验收标准**：
  - 开启「方式三：自动轮询所有策略」后，点关灯/开灯会依次发送：当前状态位变体 + 全部 6 种独立候选码
  - 三种灯光方式（状态位 / 独立码 / 混合轮询）互斥，顶部显示当前生效方式及轮询顺序
  - 配置仍持久化；`./gradlew assembleDebug test` 通过
- **规则链路**：
  - UI：`AcRemoteActivity.switchMethodHybrid` → `AcRemoteViewModel.setLightControlMethod(HYBRID_ALL)`
  - 关灯/开灯：`RemoteAction.LIGHT_*` → `RemoteControlRepository.sendLight()` → `sendHybridLight()` → 循环 `transmit()`（状态位帧 + `TclLightPatternLibrary.allCandidateIds()` 各候选）→ `AndroidIrTransmitter`
  - 顺序展示：`RemoteControlRepository.hybridAttemptSummary()` → `tvHybridSummary`
- **涉及模块**：
  - `domain/repository` — `sendHybridLight()`、`hybridAttemptSummary()`
  - `data/protocol/tcl` — `TclLightPatternLibrary.allCandidateIds()`
  - `ui/ac` — 方式三 Switch、混合轮询说明与顺序 TextView
- **改动文件**：
  - 修改：`RemoteControlRepository.kt`、`TclLightPatternLibrary.kt`、`AcRemoteViewModel.kt`、`AcRemoteActivity.kt`、`activity_ac_remote.xml`、`strings.xml`
- **对原有功能的影响**：方式一/二逻辑不变；仅新增 `HYBRID_ALL` 分支与 UI 第三开关
- **后续待办**：
  - 真机验证哪种策略生效，将有效候选标记保存后改回方式二固定使用
  - 可选：轮询间隔、成功后停止后续发送，避免一次点击改动过多空调状态

## 2026-08-17 灯光控制实验扩展：预留混合轮询模式

- **需求描述**：用户要求在“状态位变体”和“独立红外码”之外，继续覆盖更多 TCL 老机/新机差异，因此先在灯光控制配置层预留“混合轮询”模式，为后续一次按键串行尝试多种灯光策略做准备。
- **验收标准**：
  - `LightControlMethod` 支持新增 `HYBRID_ALL`
  - 不破坏现有 `STATE_BIT_VARIANT` 与 `DISCRETE_PATTERN` 配置兼容性
  - 后续可在 `Repository` / UI 层基于该枚举接入自动轮询发送逻辑
- **规则链路**：
  - 配置入口：`LightControlConfig.method`
  - 灯光策略分发：`LightControlMethod` → （后续）`RemoteControlRepository.sendLight()` 分支
- **涉及模块**：
  - `domain/model` — 灯光控制方式枚举扩展
- **改动文件**：
  - 修改：`app/src/main/java/com/universalremote/control/domain/model/LightControlConfig.kt`
- **对原有功能的影响**：无；当前仅增加枚举值，尚未改变现有发送逻辑
- **后续待办**：
  - ~~在 `RemoteControlRepository` 实现 `HYBRID_ALL` 的串行试码逻辑~~（已完成，见上条「HYBRID_ALL 自动轮询落地」）
  - ~~在 `ui/ac` 暴露「自动轮询」开关与提示~~（已完成）

## 2026-08-17 灯光控制实验：状态位变体 + 独立红外码试码

- **需求描述**：用户无原装遥控、暂不知空调型号，但开关/调温可用、灯光不可用。需在 App 内提供两种可切换试码方式，便于真机逐个排查灯光 bit/波形差异。
- **验收标准**：
  - 方式一：可在 bit 0x02 / 0x02 反向 / bit 0x40 / 0x40 反向 四种状态位变体间切换，关灯/开灯按所选变体发送
  - 方式二：可逐个试 6 种预置独立红外候选码，支持「标记有效-关/开」持久保存，并用已保存码快速发送
  - 两种方式互斥，顶部显示当前生效方式；选择持久化到 SharedPreferences
  - `./gradlew assembleDebug test` 通过
- **规则链路**：
  - 方式一（状态位）：`AcRemoteActivity`（Switch/RadioGroup）→ `AcRemoteViewModel.setLightBitVariant()` → `RemoteControlRepository.sendLight()` → `Tcl112AcRemote.apply(acState, bitMask, inverted)` → `Tcl112AcEncoder.encode()` → `AndroidIrTransmitter`
  - 方式二（独立码）：`AcRemoteActivity`（试候选/标记有效）→ `AcRemoteViewModel.sendDiscreteCandidate()` / `saveCurrentCandidateAsLightOff()` → `RemoteControlRepository` → `TclLightPatternLibrary.patternFor()` → `AndroidIrTransmitter`
  - 配置持久化：`LightControlPreferences` ← `RemoteControlRepository.persistLightConfig()`
- **涉及模块**：
  - `domain/model` — `LightControlConfig`、`LightControlMethod`、`LightBitVariant`
  - `data/preference` — `LightControlPreferences`
  - `data/protocol/tcl` — `Tcl112AcRemote`（多 bit 灯光）、`TclLightPatternLibrary`（6 种候选码）
  - `domain/repository` — 灯光发送分支、候选切换与保存
  - `ui/ac` — 灯光实验 UI 与 ViewModel
  - `core` — `AppContainer` 注入 `LightControlPreferences`
- **改动文件**：
  - 新增：`domain/model/LightControlConfig.kt`、`data/preference/LightControlPreferences.kt`、`data/protocol/tcl/TclLightPatternLibrary.kt`
  - 修改：`Tcl112AcRemote.kt`、`RemoteControlRepository.kt`、`AppContainer.kt`、`AcRemoteViewModel.kt`、`AcRemoteActivity.kt`、`res/layout/activity_ac_remote.xml`、`res/values/strings.xml`
- **对原有功能的影响**：开关/调温/模式/风速链路不变；关灯/开灯改为按用户所选方式发送（默认仍为方式一 bit 0x02）
- **后续待办**：
  - 真机标记有效码后反馈候选 ID，可设为该用户默认
  - 可选：手机 IR 接收真·学习（当前为试码+保存候选 ID）
  - 可选：根据空调型号自动预选 bit 变体

## 2026-08-17 初始化 Android 红外遥控 MVP（TCL 空调 + 关灯）

- **需求描述**：搭建万能遥控器 Android 应用首版，通过红外控制 TCL 空调，重点支持关闭面板灯光；UI 简易但预留扩展；架构采用 Google MVVM（Kotlin + ViewBinding，暂不用 Compose）。
- **验收标准**：
  - 首页展示预设设备列表，可进入空调遥控页
  - 遥控页支持：电源、温度 ±、模式、风速、**关灯/开灯**
  - 无 IR 硬件时给出提示，不崩溃
  - `./gradlew assembleDebug test` 可构建（Gradle 9.1 + AGP 9.0.1，支持本机 JDK 25）
- **规则链路**：
  - 设备列表：`MainActivity` → `MainViewModel` → `RemoteControlRepository.getPresetDevices()`
  - 空调遥控：`AcRemoteActivity` → `AcRemoteViewModel.sendAction()` → `RemoteControlRepository.sendAcAction()` → `Tcl112AcRemote.apply()` → `Tcl112AcEncoder.encode()` → `AndroidIrTransmitter.transmit()` → `ConsumerIrManager.transmit()`
- **涉及模块**：
  - `ui/main` — 设备列表
  - `ui/ac` — 空调遥控面板
  - `domain/model` — `ApplianceType`、`RemoteAction`、`AcState` 等
  - `domain/repository` — `RemoteControlRepository` 指令编排
  - `data/protocol/tcl` — TCL112AC 协议状态机与波形编码（参考 IRremoteESP8266）
  - `data/ir` — `IrTransmitter` 抽象与 `AndroidIrTransmitter`
  - `core` — `AppContainer` 轻量依赖注入
- **改动文件**：
  - 构建：`settings.gradle.kts`、`build.gradle.kts`、`gradle.properties`、`gradle/wrapper/gradle-wrapper.properties`、`app/build.gradle.kts`
  - 清单与资源：`app/src/main/AndroidManifest.xml`、`res/layout/*`、`res/values/*`、`res/drawable/*`
  - 源码：`UniversalRemoteApp.kt`、`core/AppContainer.kt`、`domain/**`、`data/**`、`ui/**`
  - 测试：`app/src/test/.../Tcl112AcRemoteTest.kt`
- **对原有功能的影响**：无（项目此前仅有循环工程脚手架，无业务源码）
- **后续待办**：
  - 真机验证 TCL112AC 红外码是否与具体空调型号匹配（协议为 Beta，不同型号可能需微调）
  - 新增更多家电类型时在 `ApplianceType` 与 `RemoteControlRepository` 扩展协议驱动
  - 可选：IR 码学习/录制、Compose 迁移
